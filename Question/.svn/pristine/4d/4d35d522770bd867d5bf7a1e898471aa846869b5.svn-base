//: Playground - noun: a place where people can play

import UIKit
import CoreGraphics

var str = "Hello, playground"


let image = UIImage(named:"group_1.png") // just for example

let value:CGFloat = 10.0

//let width:Float   = Float(image.size.width / 100.0 * value)

let colorSpace:CGColorSpace? = CGColorSpaceCreateDeviceRGB()

let bitmapInfo = CGBitmapInfo(rawValue:CGImageAlphaInfo.None.rawValue).rawValue
//let bitmapInfo = CGBitmapInfo(rawValue:CGImageAlphaInfo.PremultipliedLast.rawValue)
let rect = CGRectMake(10, 10, 10, 20);
let context = CGBitmapContextCreate(nil, Int(rect.size.width), Int(rect.size.height), 8, 0, colorSpace, bitmapInfo)


class People
{
    var name: NSString? = nil;
    var nameb: NSString! = nil;
    //var nameb: NSString = nil; //error
    
    
}
